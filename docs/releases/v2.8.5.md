# v2.8.5

A patch release. No user configuration migration is required.

Landed as [#669](https://github.com/ndycode/codex-multi-auth/pull/669) and [#661](https://github.com/ndycode/codex-multi-auth/pull/661).

## Upgrade notes

No migration is required. Install the package and confirm the version:

```bash
npm i -g codex-multi-auth
codex-multi-auth --version
```

The command should report `2.8.5`.
