# v2.8.6

A patch release. No user configuration migration is required.

Landed as [#670](https://github.com/ndycode/codex-multi-auth/pull/670) and [#671](https://github.com/ndycode/codex-multi-auth/pull/671).

## Upgrade notes

No migration is required. Install the package and confirm the version:

```bash
npm i -g codex-multi-auth
codex-multi-auth --version
```

The command should report `2.8.6`.
