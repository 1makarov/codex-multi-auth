# v2.8.7

A patch release. No user configuration migration is required.

Landed as [#674](https://github.com/ndycode/codex-multi-auth/pull/674) and [#676](https://github.com/ndycode/codex-multi-auth/pull/676).

## Upgrade notes

No migration is required. Install the package and confirm the version:

```bash
npm i -g codex-multi-auth
codex-multi-auth --version
```

The command should report `2.8.7`.
