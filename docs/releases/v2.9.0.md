# v2.9.0

A minor release. No user configuration migration is required.

Landed as [#677](https://github.com/ndycode/codex-multi-auth/pull/677) and [#678](https://github.com/ndycode/codex-multi-auth/pull/678).

## Upgrade notes

No migration is required. Install the package and confirm the version:

```bash
npm i -g codex-multi-auth
codex-multi-auth --version
```

The command should report `2.9.0`.
